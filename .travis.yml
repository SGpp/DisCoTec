language: c
cache:
  directories:
  - mpich
git:
  submodules: false
addons:
  ssh_known_hosts:
  - gitlab.mpcdf.mpg.de
  - simsgs.informatik.uni-stuttgart.de
  packages:
  - scons
before_install:
  - sh ./get_mpich.sh
script:
  - sh ./compile.sh
deploy:
  github-token: $GITHUB_TOKEN
