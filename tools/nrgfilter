#converts nrg data from format
#
#time
#data(species_1)
#data(species_2)
#...
#to
#time data(species_1)
#time data(species_2)
#...
#which can be read by GNUPLOT using e.g.
#
#plot '<nrgfilter < nrg.dat' u 1:(<nrg_val>+1) ev <n_spec>::<cur_spec> w lp
#
#where <nrg_val> is the NRG-column, <n_spec> the total number of species 
#and <cur_spec> the species to investigate (index starts at 0)
awk '{if (NF==1) {time=$1} else print time,$0}'
